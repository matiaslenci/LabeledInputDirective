import { LabeledInputDirective } from './labeled-input.directive';

describe('LabeledInputDirective', () => {
  it('should create an instance', () => {
    const directive = new LabeledInputDirective();
    expect(directive).toBeTruthy();
  });
});
